#include "../map.hpp"
#include <map>
#include <string>
#include <iostream>

using std::cout;

int main(void)
{
    ft::map<char, int> first;

    // first['a'] = 10;
    // first['b'] = 30;
    // first['c'] = 50;
    // first['d'] = 70;

    // ft::map<char, int> second(first.begin(), first.end());
    // cout << second['a'];
}